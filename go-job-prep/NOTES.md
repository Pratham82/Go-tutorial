# Notes & preferences

## Teaching preferences
- Short lessons, one tangible win each. Keep within working memory.
- Learner is a comfortable programmer in another language — do NOT re-explain
  general programming concepts; contrast with how other languages do it instead.
- Prefers retrieval practice / quizzes with immediate feedback.

## Workspace
- Workspace lives at `go-job-prep/` inside the `Go-tutorial` git repo.
- Learner's prior Go work is in sibling folders: `Tour of Go/`, `TOG-2026/`,
  `go-projects/`, `learn-go-fast/`. Covered through Methods & Interfaces.

## Open threads
- Decide a concrete portfolio project (small REST API) once error handling,
  testing, and `net/http` basics are in place.
