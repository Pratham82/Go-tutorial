package main

import (
	"fmt"
	"math"
)

// Name will epxorted if it begins with a capital letter, we have to use the same name as it was exported

func main() {
	fmt.Printf("Value of Pi is: %g\n", math.Pi)
}
